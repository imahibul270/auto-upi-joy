# -------------------------------------------------------------------
# Auto Upi — environment configuration
# Copy this file to `.env` and fill in your own Supabase project values.
# Run database/setup.sql on that project first.
# -------------------------------------------------------------------

# Used in the browser (Vite inlines these at build time)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Used during server-side rendering (same values)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key
SUPABASE_PROJECT_ID=your-project-ref

# Server-only. NEVER prefix with VITE_ and never commit a real value.
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
